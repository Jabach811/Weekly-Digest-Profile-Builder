# Contradictions · 2026-04-24

## banyan — case number collides with canam-br

- **Old (on file):** `plans-registry.csv` shows `canam-br,QK63283-00099,Canam Bridges,...`
- **New (this dump):** "Banyan Treatment Centers / Cutting Edge Staffing, that is the QK 63283 dash 00099."
- **Question:** Is Banyan's case number actually different (QK63283-000XX), or does canam-br's case number need to change? Both can't be QK63283-00099.
- **Proposed resolution:** Hold until confirmed. `banyan.md` created with `case_number: TBD (⚠ collides with canam-br per 2026-04-22 registry)`. Registry row for banyan left out until you confirm.

## callidi — DOB/DOH open item implicitly closed but not named

- **Old (on file):** `caseload/callidi.md` Open Items include `[ ] nudge client for DOB + DOH`.
- **New (this dump):** "Balances and loans are ready to be imported. In process. We just need Informatica to open up."
- **Question:** The DOB/DOH must have come in for balances to be staged — is that open item now closed, or are we staging around it?
- **Proposed resolution:** Hold until confirmed. Leaving `[ ] DOB + DOH` as open in the file with a dated note that it appears resolved but operator hasn't said so explicitly.

---

## tmg — vesting-fix approach pivot (ask client → self-calculate + verify)

- **Old (on file):** `caseload/tmg.md` open items include `[ ] Ask Meredith for participants' years-of-service` and `[ ] Load YOS once received` — the planned fix path was a client-data ask.
- **New (this dump, BD4):** "Simply calculate years of service on our own without asking the client, and verify our numbers against the vendor data. Once I did that, everyone we expected to be 100% turned out to be 100% on the vendor records." Then BD5: "Import years of service, pop that in" (referring to self-calculated YOS).
- **Question:** Drop the Meredith ask entirely and proceed with self-calculated YOS, vendor-verified? Or still loop her in for a subset (e.g., ambiguous cases, or just the one self-flagged participant)?
- **Proposed resolution:** Hold the ask-Meredith open item as `[~]` with a note that the approach is pivoting. Don't delete it. New file content reflects the pivot proposal but doesn't drop the original until you confirm.

---

## callidi — loan issue identity (frozen/unfrozen vs. XKB/XKC missing from Transact Detail)

- **Old (on file):** `caseload/callidi.md` open items include `[ ] Diagnose the loan order discrepancy — frozen/unfrozen history, decide whether to re-amortize.` (from 4/22 — one participant, one loan, possibly needs re-amortization).
- **New (this dump, BD3):** A different-sounding loan problem surfaced today during the Beth walkthrough — ref numbers **XKB and XKC** are present in Bill Remit Detail but not landing in Transact Detail. Joel suspects fund-letter-prefix collision (alphabet rolling past Z into AA/BA/CA series). Doesn't sound like the same thing as a re-amortization on a frozen/unfrozen loan.
- **Question:** Is the 4/22 frozen/unfrozen issue the **same** problem the XKB/XKC discovery now explains, or are these two distinct loan issues both still open?
- **Proposed resolution:** Hold both as open in `callidi.md`. Don't merge them until you confirm. If they're the same, we can collapse to one item; if not, both need separate resolutions.

---

## ~~general — NBI mention is ambiguous~~ RESOLVED 2026-04-26

- **Old (on file):** `caseload/haverhill.md` open item `[ ] Beth · send NBI status email`. Separately `projects/nbi-search.md` is a ready-to-ship app where Joel was about to push an update.
- **New (this dump, BD5):** "Having troubles with NBI at the moment — not sure what's going on. Probably won't be able to push my update out, but good thing is it's in there. It matches, so we can continue forward."
- **Question:** Is the "NBI" he's having trouble with the **NBI Search app** (project — pushing a code update) or the **Haverhill NBI status email** (caseload — sending status to client)?
- **RESOLVED (2026-04-26 dump):** "NBI was down" = external NBI system (company-wide; new field broke it). Beth tried to send the Haverhill status email and couldn't. This is the external email service, NOT the NBI Search app. The app's push-stuck issue is a separate, unrelated problem. `projects/nbi-search.md` `ambiguous-with-haverhill-nbi` flag removed.
